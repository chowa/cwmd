const options = {
    siteName: 'chowa markdown',
    rootPath: '',
    compilePath: '.cwmd',
    distPath: 'docs',
    port: 10666,
    host: '127.0.0.1',
    themeFile: null,
    faviconFile: null,
    logoFile: null,
    navFile: null
};

export function mergeOptions() {
    // todo
}

export default options;
